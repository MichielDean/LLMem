{
  "description": "LLMem memory system — injects context at session start and preserves key memories during compaction.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "llmem stats 2>/dev/null && echo '---' && llmem search 'recent decisions important facts preferences' --limit 5 2>/dev/null",
            "timeout": 30
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "llmem search 'recent decisions important facts preferences procedures' --limit 15 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}